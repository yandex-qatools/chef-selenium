description "Selenium-grid ghostdriver"

start on runlevel [345]
stop on shutdown

# Automatically restart process if crashed
respawn

setuid selenium

exec <%= @phantomjs %> --webdriver=<%= @gdport %> --webdriver-selenium-grid-hub=http://<%= @host %>:<%= @port %> >> <%= @log %> 2>&1
